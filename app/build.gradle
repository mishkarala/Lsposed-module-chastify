plugins {
    id 'com.android.application'
}

android {
    namespace 'com.example.bridge'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.bridge"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // Это единственная правильная и рабочая ссылка для современных модулей
    compileOnly 'org.lsposed.xposed:api:82'
}
